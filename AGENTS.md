# 项目工作说明

## 沟通方式

- 用户是一位 80 岁老人，尽量用中文，用最通俗的话说明。
- 除非必须确认危险操作，否则不要频繁打扰用户。
- 如需通过浏览器询问 GPT Pro，问题开头必须标明本项目路径 `C:\Users\WHR\Documents\策略迭代` 和本线程目标，避免和其他 Codex 线程混淆。
- 不混用其他线程的浏览器结论、文件路径或运行结果。

## 长期研究任务

本项目长期目标是继续研究 BTC 15m 策略。当前已经固化一个满足历史条件的研究版；后续如果继续优化，只能另起新版本，不能覆盖当前固化版。历史回测目标条件是：

- 2025 年收益率大于 100%。
- 2026 年收益率大于 100%。
- 不能使用未来函数，信号只能用当时已经收盘的数据。
- 市价开仓和平仓的总交易成本按 0.2% 计算。
- 历史回测中每个月都盈利。
- 每个月交易次数最低 10 次。

当前已固化研究版定位为 `0号策略`：`monthly_profit_lock_research_freeze_20260627`，见 `STRATEGY_0.md`、`CURRENT_STRATEGY_FREEZE.md` 和 `artifacts/strategy_freeze_monthly_profit_lock_20260627/freeze.json`。以后改参数、手续费、规则或数据时，不要覆盖这个固化版，要另起新编号、新文件夹。

0号策略是永久保留的基准策略。后续优化要复制或另开目录，例如 `artifacts/strategy_1_.../`，不能在 0号策略原目录里直接覆盖。

## GitHub 与 GPT Pro 协作

- 本项目身份：`C:\Users\WHR\Documents\策略迭代`，BTC 15m 策略迭代研究。
- GitHub 仓库：`https://github.com/yw9522872-debug/btc-strategy-iteration-20260627`。
- GitHub 仓库只用于同步本项目文件和给 GPT Pro 读取，不要混用其他项目或其他 Codex 线程的仓库。
- 给 GPT Pro 的问题开头必须同时写本地路径和 GitHub 仓库 URL。
- GPT Pro 优先阅读 `GPT_PRO_REVIEW_BRIEF.md`，再看 `AGENTS.md`、`scripts/` 和 `artifacts/*/summary.json`。
- 推送前先检查不要包含 API key、secret、token、`.env` 或实盘下单配置。

## 工作边界

- 这里只做研究、回测、检查，不下实盘订单。
- 不读取、不打印、不保存 API key、secret、token 或 `.env`。
- 不启动实盘 supervisor。
- 不改真实仓位。

## 验证要求

- 任何候选策略都要输出年度、月度、交易次数和手续费口径。
- 发现结果太好时，优先检查是否有未来函数或标签泄漏。
- “每个月都盈利”只能表示历史回测月份盈利，不能承诺未来真实市场一定盈利。

## 本次开发需要记住

- 当前固化版历史上满足任务条件，但参数是事后从历史里挑出来的，过拟合风险高；不能把它说成稳健实盘策略。
- 已做过 2024 选参、2025/2026 验证：这个更严格检查没有达到每年 100%，说明过拟合风险是真问题。
- 当前固化版执行逻辑没有发现明显未来函数：信号只用已收盘K线，下一根K线才吃收益；月度锁利只用当月已经发生的收益和交易次数。
- 手续费口径固定为开平合计 0.2%，代码里是单边 `0.001`。
- 2026 主结果用本地数据到 `2026-06-19 23:45 UTC`；另有 Binance 补测到 `2026-06-26 18:30 UTC`，补测后月内已锁利空仓。
- 胜率默认口径是“持仓中的 15分钟K线正收益占比”，不是严格的一笔一笔完整交易胜率。
- 14号发现旧 `event_entry_fullscan` 对齐 Binance USD-M futures 公共K线，不是 spot；用 2020-2026 更严格滚动审计后，`ret_state 64/100` 家族判定为 `STOP_FAMILY`，不要继续手工堆补丁。
- 15号已确认统一 futures K线底座可用：`2020-01-01 00:00 UTC` 到 `2026-05-31 23:45 UTC`，`224928` 根15分钟K线，重复/断档/OHLC异常/缺失月份均为 `0`。后续新策略族优先基于这个底座。
- 16号在15号底座上扫了简单新策略族（均线/Donchian/RSI/布林带/ATR突破），严格逐月选择没有硬通过，144个静态候选事后硬通过数量也是 `0`。不要把16号升级候选；下一步更适合做上限测试。
- 17号对16号候选做了“看答案”的月度上限测试，仍然失败：最宽松上限还有 `6` 个不盈利月份，要求每月交易不少于 `10` 后有 `10` 个不盈利月份。结论 `SIMPLE_FAMILY_UPPER_BOUND_FAILS`，不要继续扩这批简单规则。
- 18号复盘17号失败月份：`6` 个月是144个候选没有任何正收益，`4` 个月是有正收益但交易次数不足10。结论 `FAILURE_MONTHS_EXPLAIN_SIMPLE_FAMILY_STOP`，说明这批简单规则不值得继续微调。
- 19号测试日历/星期/小时季节性，严格逐月选择也失败：最好 `all_calendar` 为 2023 `-1.32%`、2024 `+71.64%`、2025 `-26.91%`、2026 YTD `+43.60%`，亏损月 `22`。结论 `CALENDAR_SEASONALITY_FAILS`，不要升级候选。
- 20号测试 OHLC K线实体/影线/振幅/波动结构的“看答案”上限，仍然失败：189个候选静态硬通过为 `0`；最宽松月度上限 2023 `+51.61%`、2024 `+18.35%`、2025 `+12.99%`、2026 YTD `+0.18%`，仍有 `30` 个不盈利月份；要求每月10单后 2024/2025/2026 都亏。结论 `OHLC_STRUCTURE_UPPER_BOUND_FAILS`，不要升级这批 OHLC 结构规则。
- 21号补 Binance USD-M futures 15m 成交量/taker 字段做“看答案”上限，仍然失败：378个候选静态硬通过为 `0`；最宽松月度上限 2023 `+45.98%`、2024 `+54.74%`、2025 `-12.01%`、2026 YTD `-9.17%`，仍有 `25` 个不盈利月份；要求每月10单后有 `29` 个不盈利月份。结论 `VOLUME_UPPER_BOUND_FAILS`，不要升级这批成交量小规则。
- 22号把 16/19/20/21 的 `927` 个候选放入同一张硬目标压力表：严格逐月选择器 `0/120` 通过；原始硬目标下，月度oracle也没过，差 `2023-07` 和 `2024-06` 两个月。结论 `ORIGINAL_TARGET_AND_SELECTION_BOTTLENECK`。不要继续扩 16/19/20/21 小规则；若继续研究，只能换真正不同的数据源，或先把目标改得更现实。
- 23号测试 Binance USD-M futures 资金费率这个新数据源的“看答案”上限：246个资金费率候选静态硬通过为 `0`，但月度oracle带每月10单门槛能过，2023 `+17263.32%`、2024 `+45467.21%`、2025 `+6801.02%`、2026 YTD `+648.74%`，亏损月 `0`。结论 `FUNDING_RATE_UPPER_BOUND_HAS_MONTHLY_PIECES`，但不能交易。
- 24号复用23号资金费率候选做严格逐月选择，失败：最好 `funding_mean_only` 为 2023 `-22.82%`、2024 `-42.05%`、2025 `+9.70%`、2026 YTD `+10.95%`，亏损月 `20`。结论 `FUNDING_RATE_STRICT_SELECTOR_FAILS`。不要升级资金费率候选；若继续，应换持仓量等新数据源先做上限测试，或调整硬目标。
- 25号检查 Binance 公开持仓量数据能否做 2020-2026 上限测试，结论 `OPEN_INTEREST_HISTORY_NOT_AVAILABLE_FOR_2020_2026`：openInterestHist 最近数据可取，但 2020-01、2023-01 历史请求返回 `startTime invalid`，公开接口只够最近1个月观察，不够做 2023-2026 硬目标上限。不要用不完整持仓量硬做历史上限。
- `DATA_SOURCE_OPEN_INTEREST_LONG_SHORT_REVIEW_20260627.md` 已审查持仓量/多空比历史数据源：Binance 官方持仓量只保留最近1个月，多空比只保留最近30天；首选 Tardis.dev，其次 CoinGlass/Amberdata。拿到完整 CSV 前，不要硬做持仓量/多空比多年回测；拿到后先做数据质量审计，再做上限测试。
- 26号改用 Binance 免费公开 1分钟 USD-M futures K线测试15分钟内部结构上限，结论 `INTRABAR_1M_UPPER_BOUND_FAILS`：2020-01到2026-05 的1分钟月包完整，缺失/重复/断档均为 `0`；有 `8` 根15分钟K线和15号底座OHLC不一致，已禁用这些K线信号。186个候选静态硬通过为 `0`；最好看答案上限 2023 `+47.27%`、2024 `+16.29%`、2025 `-3.27%`、2026 YTD `-7.68%`，亏损月 `28`；要求每月10单后 2025/2026 仍亏。不要继续扩这批1分钟内部结构小规则。
- 27号目标可行性审计结论 `TARGET_RELAXATION_HELPS_ORACLE_NOT_SELECTOR`：复用22号927个候选，测试年收益30/50/80/100、月度不限/不低于-2%/不低于-1%/>0、月交易0/5/10。严格逐月选择器 `0/49` 通过；看答案 oracle `37/49` 通过。原手续费0.2%下，看答案最接近原目标的通过口径是年100%、每月10单、月亏不低于-1%；但严格选择器仍失败。不要继续加免费K线小规则；若不拿真正不同的新数据，应把目标降到影子跟踪/低年化验证。
- 28号不要求月月盈利审计结论 `NO_STRICT_RELAXED_UPGRADE`：复用14号 `ret_state 64/100` 家族和统一期货K线，不新增规则、不新增数据。去掉每月必须盈利后，最好的严格不看未来选择器仍只有 2023 `-21.96%`、2024 `+140.23%`、2025 `-5.28%`、2026 YTD `-0.35%`，亏损月份 `6`，最大回撤 `-58.39%`。看答案月度 oracle 可以做到 2025 `+1692.64%`、2026 YTD `+171.90%`，但这是未来函数，不能交易。结论：问题不是月月盈利条件单独太严，而是当月之前选不出正确候选。
- 29号按 GPT Pro 建议检查免费 raw trade 数据覆盖，结论 `FREE_SPOT_PERP_RAW_TRADE_DATA_AVAILABLE`：Binance 免费公开 futures/spot `aggTrades`、`trades`、1m K线、fundingRate、markPriceKlines、indexPriceKlines、premiumIndexKlines 从 `2020-01` 到 `2026-05` 均为 `77/77` 个月可用；bookTicker 月包探针 2020-01 返回 `404`，不要做秒级盘口主策略。下一步若继续，另起30号，只做 spot-perp `aggTrades` 成交流错位上限测试，funding/mark/index/premium 只做过滤器。
- 30号 spot-perp `aggTrades` 样本上限测试结论 `SPOT_PERP_AGGTRADE_SAMPLE_UPPER_BOUND_FAILS`：只下载四个关键样本月 `2023-07`、`2024-06`、`2025-08`、`2026-05` 的 spot/futures `aggTrades`，压缩包共约 `2.586 GB`，聚合后原始zip已删除。数据质量通过：`11808` 根15分钟特征，spot/futures 缺失15分钟行均为 `0`。204个现货-合约成交流错位候选里，不要求交易次数时最好只是 `0%` 不交易；要求每月10单后，看答案 oracle 四个样本月全部亏，样本总收益 `-25.33%`，最差月 `-12.67%`，最少月交易 `39`。不要下载全量约90GB继续做这条免费 aggTrades lead-lag 路线。
- 31号多币种免费期货样本上限测试结论 `MULTISYMBOL_FREE_FUTURES_SAMPLE_UPPER_BOUND_HAS_SIGNAL`：样本月同为 `2023-07`、`2024-06`、`2025-08`、`2026-05`；币种为 BTC/ETH/SOL/BNB/HYPE/DOGE/XRP/ADA/AVAX/LINK 的 Binance USD-M futures 15m K线。HYPE 只覆盖2个样本月，DOGE覆盖3个，其余主要币覆盖4个。816个跨币种强弱/单币动量反转候选里，18个静态候选四个样本月都为正；每月10单 oracle 四个样本月全正，最差月 `+244.10%`，最少月交易 `18`，但收益数字极端且是看答案，不能交易。下一步若继续，应该另起32号做完整历史覆盖和严格逐月选择器；不要直接把31号当候选策略。
- 32号 BTC 3m 2025到最新公开数据上限测试结论 `BTC_3M_2025_TODAY_UPPER_BOUND_FAILS`：Binance USD-M futures BTCUSDT 3m K线从 `2025-01-01 00:00 UTC` 到 `2026-06-27 23:57 UTC`，共 `260640` 根，重复/断档/补齐均为 `0`；`2026-06-28` 日包运行时尚未公开。348个 BTC 单币3m候选静态每月全正数量为 `0`；要求每月10单的看答案 oracle 为 2025 `+51.33%`、2026 YTD `+227.11%`，但不盈利月份 `11` 个，最差月 `-3.67%`。不要继续单币 BTC 3m 小规则；如果继续，优先回到31号提示的多币种完整历史严格选择器。
- 33号多币种完整历史严格选择器结论 `MULTISYMBOL_ORACLE_HAS_PIECES_BUT_STRICT_SELECTOR_FAILS`：Binance USD-M futures 15m 月包覆盖 BTC/ETH/SOL/BNB/DOGE/XRP/ADA/AVAX/LINK，`2020-01-01 00:00 UTC` 到 `2026-05-31 23:45 UTC`，共 `224928` 根15分钟主时间轴，重复/断档均为 `0`。744个候选静态硬通过数量为 `0`；每月10单看答案 oracle 能过，但严格逐月选择器最好 `all_multisymbol` 只有 2023 `+17.72%`、2024 `-21.17%`、2025 `-46.85%`、2026 YTD `-5.64%`，不盈利月份 `21`，最少月交易 `1`。结论：31号样本信号扩到完整历史后仍是看答案才能选中，不能升级候选；不要继续手工扩这批多币种免费K线小规则。
- 34号多币种失败根因审计结论 `ROOT_CAUSE_UNSTABLE_HINDSIGHT_SELECTION`：复用33号结果，不新增交易规则、不重新下载数据。41个评估月里，每个月都有“每月10单且正收益”的看答案候选，但严格选择器有 `33` 个不达标月份；训练期硬通过候选数量每个月都是 `0`；当月 oracle 赢家在月初训练排序中位名次为 `222`，排进前10的月份为 `0`；跟随上月 oracle 赢家在2025为 `-99.9983%`、2026 YTD为 `-96.7508%`。失败主因不是手续费小补丁，而是赢家换得太快，过去表现很难提前选中当月赢家。不要先加交易规则；若继续，只能另起新编号测试真正不同的选择方法，并先做上限/泄漏审计。
- 35号旧 BTC 3m 项目灵感复盘结论 `OLD_BTC_3M_GIVES_FRAMEWORK_NOT_DEPLOYABLE_RULES`：按用户要求查看 `C:\Users\WHR\Documents\BTC多因子研究_20260626`。旧项目保存的 2025-2026 样本内 BTC 3m 组合为 2025 `+145.67%`、2026 YTD `+104.09%`、18个月全正、最少月交易 `340`，标签收益与独立3m价格重放差异 `0` bps；但同一规则压到2024为 `-211.74%`、正收益月份 `2/12`，pre-2025 锁参和 pre-2025 ML 路由通过行数均为 `0`。结论：可借多周期特征、funding/premium 可用时间、事件池、资金约束和逐K盯市框架；不能照搬旧7条规则、旧阈值、10x毛暴露或样本内选择结果。
- 36号多币种候选组合选择器审计结论 `ENSEMBLE_SELECTOR_ON_33_CANDIDATES_FAILS`：不下载新数据、不新增交易规则，只复用33号候选月度结果，测试180组严格多规则等权组合。严格通过配置数 `0`；最好配置 top_k `50`、lookback `24`、min_pos_rate `0.45`、score `pos_mean`，2023 `-14.30%`、2024 `-26.26%`、2025 `+0.06%`、2026 YTD `-22.93%`，仍有 `24` 个不盈利月份，最小月交易 `546`。结论：旧项目“组合多个规则”的近路不能直接救回33号免费15m候选池；不要继续调33号组合参数。若继续追旧项目灵感，应另开更重的3m/多周期事件池数据审计，并继续严格不看未来。
- 37号 BTC 3m 多周期事件池审计结论 `BTC_3M_MULTITIMEFRAME_EVENT_POOL_FAILS`：Binance USD-M futures BTCUSDT 3m 月包 `2020-01-01 00:00 UTC` 到 `2026-05-31 23:57 UTC`，`1124640` 根3分钟K线，重复/断档/缺失月份均为 `0`。496个事件候选静态硬通过为 `0`；每月10单看答案 oracle 2025 `+503.35%`、2026 YTD `+157.89%`，但仍有 `2024-04`、`2025-10` 两个不盈利月份；严格选择器最好 `range_events` 为 2023 `-2.17%`、2024 `-8.69%`、2025 `-6.49%`、2026 YTD `-7.45%`，不盈利月份 `27`，最少月交易 `2`。结论：不要继续小修这批 BTC 3m 多周期事件；除非换真正不同的数据源，否则更适合降低目标做影子跟踪。
- 38号强行过拟合 Alpha 挖掘审计结论 `FORCED_OVERFIT_ALPHA_CLUES_NOT_YET_TRADEABLE`：不下载新数据、不新增交易规则，只复用33号和37号候选月度结果，每月故意看答案挑当月收益最高且至少10单的候选。合并看答案结果 2025 `+17665719.09%`、2026 YTD `+4414.40%`、不盈利月份 `0`、最差月 `+25.64%`、最少月交易 `14`，但这是未来函数，不能交易。赢家 `41/41` 个月都来自33号，主要是多币种 `single_symbol` 的4倍动量/反转，常见 AVAX/SOL/LINK/XRP/DOGE/ADA；当月赢家在月初训练排序中位名次 `231`，进前10月份 `0`，跟随上月赢家在2025和2026都接近归零。结论：38号只能提供线索，不能升级策略；若继续，应另起39号研究不用未来数据提前识别这些多币种赢家结构。
- 39号 Alpha 规律挖掘结论 `ALPHA_PATTERN_FOUND_BUT_SIMPLE_SELECTOR_STILL_FAILS`：不下载新数据、不新增交易规则，只拆解38号赢家并测试简单不看未来搬运。规律明确：历史赢家集中在多币种山寨币、`4倍`、`single_symbol` 单币动量/反转，`384` 根15分钟窗口最多；常见 AVAX/SOL/LINK/XRP/DOGE/ADA/ETH；上月涨跌幅绝对值排前5占 `72.5%`，上月波动率排前5占 `72.5%`。但简单月初选择器不能同时让2025和2026盈利，最好 `prev_abs_top2_reversal` 为 2023 `+100.22%`、2024 `-40.50%`、2025 `0.00%`、2026 YTD `+18.01%`。结论：确实发现了历史赢家形状，但免费K线简单选择还不能变成可交易盈利策略；后续应找 funding/premium/成交流/持仓量/多空比这类提前识别信号。
- 40号多币种 Funding 提前识别审计结论 `FUNDING_SIGNAL_WEAK_NOT_TRADEABLE`：新下载 Binance 免费 fundingRate REST 历史，只覆盖39号常见赢家币 ETH/SOL/DOGE/XRP/ADA/AVAX/LINK，`2022-12` 到 `2026-05` 每币 `3834` 行、`42` 个月、重复时间戳 `0`、非8小时间隔 `0`。Funding 对38号赢家有弱解释力：赢家上月 funding 绝对值排名中位数 `3.5`，排前3比例 `50.0%`，上月 funding 为正比例 `82.5%`。最好不看未来选择器 `hot_abs_funding_abs_both` 为 2023 `+6662.64%`、2024 `-90.19%`、2025 `+203.32%`、2026 YTD `+18.01%`，交易月份 `17`，亏损交易月份 `6`。结论：funding 能把39号规律推进一步，但不能达到原始硬目标；不要继续扩 funding-only 小规则，若继续应测 premium 或成交流。
- 41号 BTC+HYPE 放宽门槛审计结论 `BTC_HYPE_DRAWDOWN_CAPPED_ORACLE_ONLY_PASSES`：只用 BTCUSDT/HYPEUSDT，去掉月月盈利和每月交易次数要求，只保留 2025/2026 YTD 都超 `100%`、最大回撤不超过 `50%`。HYPE 免费15m历史只从 `2025-06-01` 开始。1008个候选里静态固定参数通过数 `0`；普通看答案 oracle 收益很高但最大回撤 `-100%`；回撤限制版看答案 oracle 2025 `+4176.22%`、2026 YTD `+100.67%`、最大回撤 `-49.98%`，能过放宽门槛但看了当月答案；严格不看未来选择器 2025 `-16.27%`、2026 YTD `-35.55%`、最大回撤 `-40.04%`。结论：历史上能拼出 BTC+HYPE 放宽目标曲线，但还没有可交易选择器；不要把41号升级策略。
- 42号 BTC+HYPE 状态可预测性审计结论 `BTC_HYPE_STATE_FEATURES_FAIL_FIRST_PASS`：按 GPT Pro 建议先做轻量 42-1，不下载大 `aggTrades`，只用 Binance 免费 REST `klines/fundingRate/premiumIndexKlines/markPriceKlines` 测 2025-06 到 2026-05。用月初已知状态给41号1008个BTC+HYPE候选打分，top20 包含41号安全oracle赢家比例只有 `33.33%`；top20普通看答案上限 2025 `+254.72%`、2026 YTD `+129.73%` 但最大回撤 `-99.999%`；回撤过滤后 top20 看答案上限 2025 `+191.46%`、2026 YTD `+36.17%`、最大回撤 `-47.88%`；严格top1为2025 `+29.45%`、2026 YTD `-32.95%`、最大回撤 `-99.999%`。结论：月初状态打分近路失败；若继续只能测更细的动态门控，不能把42号升级策略。
- 43号 BTC+HYPE 尾部事件归因审计结论 `TAIL_EVENTS_DOMINATE_ORACLE_PNL`：按 GPT Pro 第二轮建议复用41号回撤限制版oracle逐K收益和42号BTC/HYPE 15m数据，检查利润是否集中在极端事件附近。事件定义：HYPE 4小时绝对涨跌幅不低于 `5%`，或24小时绝对涨跌幅不低于 `12%`，或HYPE相对BTC 4小时残差z值绝对值不低于 `2.5`；事件窗口为前后各48小时。结果：极端事件原始K线占比 `6.20%`，事件窗口占比 `79.05%`，正收益log里 `90.44%` 落在事件窗口，事件窗口内净log `+4.7580`，窗口外净log `-0.3059`。结论：41号oracle利润确实和尾部事件相伴，但48小时窗口很宽，只能说明尾部事件方向值得继续证伪，不能升级策略；若继续另起44号做事件后 action oracle。
- 44号 BTC+HYPE 尾部事件后动作 oracle 结论 `TAIL_EVENT_ACTION_ORACLE_PASSES_RELAXED_GATE`：事件检测只用已收盘过去K线，但事件后的动作选择看未来。最好配置事件阈值为 HYPE 4小时绝对涨跌 `4%`、24小时绝对涨跌 `10%`、HYPE/BTC 4小时残差z绝对值 `2.0`，min_gap `16`。结果 2025 `+2349453758140.50%`、2026 YTD `+3774017741.59%`、最大回撤 `-31.94%`、交易 `161` 笔。动作主要是 HYPE 反转 `63` 笔、HYPE 顺势 `62` 笔。结论：HYPE 极端事件后有足够历史动作空间，但44号仍是未来 oracle，不能交易。
- 45号 BTC+HYPE 尾部事件强过拟合策略结论 `TAIL_EVENT_FITTED_POLICY_PASSES_IN_SAMPLE_RELAXED_GATE`：按用户要求强行把44号未来 oracle 标签拟合成决策树。最好树使用 `market_only` 特征，深度 `16`、叶子 `100`、训练准确率 `100%`，同段历史 2025 `+2349453758140.50%`、2026 YTD `+3774017741.59%`、最大回撤 `-31.94%`、交易 `161` 笔，过放宽门槛。但训练标签来自同段未来 oracle，属于强过拟合，不是实盘策略。找到的线索是：HYPE/BTC 极端事件后，顺势/反转切换、HYPE/BTC 相对残差、4天趋势、24小时涨跌、premium/funding 都有解释力。下一步如继续，应另起46号做严格走步：每个月只用过去事件训练，再测下个月。
- 46号 BTC+HYPE 尾部事件严格走步验证结论 `TAIL_EVENT_WALKFORWARD_POLICY_FAILS_RELAXED_GATE`：每个月只用以前月份的44号oracle标签训练树，再测当前月。最好严格走步为 `market_plus_time`、树深度 `5`、最小叶子样本 `3`，2025 `+230.13%`、2026 YTD `+865.81%`，但最大回撤 `-84.48%`，交易 `130` 笔，未过最大回撤不超过50%的放宽门槛。临时试简单回撤刹车也没救：回撤压住时收益掉太多，收益够时回撤仍过大。结论：45号找到了历史过拟合规律，但还没验证成稳定 alpha，不能升级实盘。
- 47号 BTC+HYPE 尾部事件因果风控覆盖审计结论 `TAIL_EVENT_CAUSAL_RISK_OVERLAY_PASSES_RELAXED_GATE_IN_SAMPLE`：在46号严格走步动作上加逐笔因果风控，风控只用交易已发生盈亏。扫描 `7560` 个配置，`99` 个过放宽门槛。最好配置 `market_plus_time`、树深度 `5`、最小叶子 `3`、仓位缩放 `0.8`、跟踪止损 `-10%`、止盈 `20%`，2025 `+281.55%`、2026 YTD `+226.41%`、最大回撤 `-45.23%`、交易 `130` 笔。不带时间字段的 `market_only` 也能过：树深度 `6`、最小叶子 `3`、仓位缩放 `0.65`、跟踪止损 `-8%`、止盈 `50%`，2025 `+180.30%`、2026 YTD `+222.40%`、最大回撤 `-42.22%`、交易 `124` 笔。47号比45/46更接近可交易逻辑，但风控参数仍在同段历史里选出，不能直接实盘；下一步应冻结47号参数做48号影子/留后验证。
- 49号 BTC+HYPE 冻结47号参数最新公开数据验证结论 `FROZEN_47_LATEST_PARTIAL_MONTH_FAILS`：48号编号本地已被公开 Jesse 代理回测占用，所以 BTC+HYPE 冻结验证顺延为49号。49号冻结47号最好配置和 market_only 配置，不在最新数据上重新调参，拉取 Binance 公共 REST 到 `2026-06-29 15:00 UTC` 已收盘15分钟K线，BTC/HYPE 行数各 `5726`、重复 `0`、缺失 `0`。47号最好配置在 2026-06 当前为 `-14.09%`、最大回撤 `-36.31%`、交易 `13`；market_only 配置为 `-23.44%`、最大回撤 `-46.38%`、交易 `13`。结论：47号不能进实盘/testnet，最新未调参数据已经失败；下一步如继续只能做50号失败归因，不要直接用2026-06重新调参。
- 50号 2C 去掉月度锁利审计结论 `MONTHLY_LOCK_IS_CRITICAL_FOR_2C`：按用户要求复用2C，只去掉“月度盈利后停手/post-lock runner”，并额外测试连未满10单前临时降仓也去掉。两个版本都失败：`no_lock_keep_quota` 为2025 `-99.97%`、2026 `-11.19%`、最大回撤 `-99.99%`；`no_lock_no_quota` 为2025 `-99.98%`、2026 `+131.38%`、最大回撤接近 `-100%`。去掉锁利后暴露率约 `98%`，换手升到 `9000+` 到 `11000+`，账户被反复交易和反向行情打穿。不要把“直接去掉2C月度锁利”作为升级方向。
- 51号 2C 锁利前交易次数敏感性审计结论 `TEN_TRADE_MONTHLY_LOCK_IS_A_FRAGILE_PART_OF_2C`：按用户质疑检查2C是否过度依赖“每月10笔后锁利”。保持2C大部分逻辑不变，只把允许锁利前的最低交易次数从 `10` 提到 `20/30/50/100`，并加完全不锁利对照。只有 `10` 笔版本通过；`20` 笔版本已变成2025 `-84.39%`、最大回撤 `-95.46%`；`30/50/100` 和不锁利基本都是接近打穿。结论：用户判断成立，2C不是可以放心放开的强策略，而是高度依赖早锁利的历史候选；后续不要继续围绕旧2C/ret_state家族小修小补。
- 52号 2C 前10笔与10笔后归因结论 `POST_10_TRADES_EXPOSES_WEAK_RAW_SIGNAL`：复用50号不锁利但保留临时降仓的逐K回放，把每个月拆成前10笔交易区域和10笔之后区域。前10笔区域为2025 `+183.19%`、2026 `+216.76%`，亏损月份 `2`；10笔之后区域为2025 `-99.99%`、2026 `-71.96%`，亏损月份 `14`，换手和暴露都很高。结论：用户判断成立，10笔锁利不是优势，而是遮住了后续交易区间质量很差；后续如研究2C，只能研究“月初/前10笔收益集中是否可被提前识别”，不要把2C当成可长期暴露的强alpha。
- 53号 2C 每月只做前N笔审计结论 `FIRST_N_ONLY_HAS_RELAXED_SIGNAL_NOT_ORIGINAL_PASS`：复用2C逻辑，不看月度利润阈值，只规定每个月最多做前 `5/10/15/20` 笔订单后无条件空仓。原始“月月盈利”硬目标没有任何版本通过；但放宽到2025/2026都超100%、最大回撤不超过50%时，`first_5_orders_then_flat` 和 `first_10_orders_then_flat` 通过。`first_10` 为2025 `+202.08%`、2026 `+261.07%`、最大回撤 `-29.25%`，但有 `2` 个亏损月。结论：52号“前段窗口”不是纯利润阈值幻觉，但仍是事后发现的历史线索，不能直接升级；若继续，应冻结 first5/first10 做更早历史和最新公开数据验证，不再继续调N。
- 54号 2C 核心信号失败归因结论 `CORE_RET_STATE_SIGNAL_NOT_GOOD_ENOUGH`：按用户纠正，不再看开仓笔数，直接检查旧2C/`ret_state 64/100` 核心信号本身。核心信号1倍无手续费不锁利为2025 `-4.90%`、2026 `+100.48%`；1倍正常手续费为2025 `-48.95%`、2026 `+50.61%`；8倍正常手续费为2025 `-99.54%`、最大回撤接近 `-100%`；2C保护逻辑不早停也接近打穿。单根K线方向预测2025为 `-4.90%`、胜率 `49.53%`、平均边际 `-0.0143 bps`。结论：问题不是笔数，而是旧2C/ret_state核心信号不够好；后续不要继续围绕旧2C/ret_state调锁利、调笔数、调小保护。
- 55号 BTC/HYPE 尾部事件核心信号审计结论 `TAIL_EVENT_CORE_DRAWDOWN_CAPPED_ORACLE_ONLY_PASSES`：按用户要求换核心信号，不再修旧2C/ret_state。55号用 BTC/HYPE 尾部事件触发：HYPE 4小时/24小时大波动、HYPE 相对 BTC 4小时残差z极端；固定动作包括 HYPE/BTC 顺势、反转和配对动作。覆盖 `2025-06` 到 `2026-06-29 15:15 UTC`，144个候选。静态固定候选0个过放宽门槛，最好静态为2025 `+34.83%`、2026 `+104.15%`、最大回撤 `-71.97%`；严格选择器为2025 `-11.64%`、2026 `-0.41%`；但回撤限制版看答案oracle为2025 `+1689.67%`、2026 `+1047.55%`、最大回撤 `-40.66%`、亏损月份0。结论：尾部事件后有风险受控空间，但还不能提前选对动作，不能升级策略；若继续，应研究事件后不用未来数据判断顺势/反转。
- 56号 尾部事件亏损根因审计结论 `LOSS_ROOT_CAUSE_UNSTABLE_ACTION_SELECTION`：复用55号结果，不新增候选、不调参数，专门拆亏损原因。根因不是没有事件机会，而是事件后正确动作每月切换太快：13个oracle月份用了10个不同候选，动作切换10次；oracle动作分布为HYPE顺势6、HYPE反转4、BTC顺势2、BTC反转1。严格选择器0个月选中同一候选，只2个月选中同一种动作，10个月错过正收益oracle；oracle赢家在训练期排序中位名次为98，12个月里0次排进前10；跟随上月oracle赢家只有5/12个月为正，简单收益求和+22.81%，而当月oracle简单收益求和+652.46%。结论：不能靠过去月度收益排名选动作；若继续，应做事件当下状态特征的动作选择可预测性审计。
- 57号 尾部事件状态动作可预测性审计结论 `EVENT_STATE_LABEL_ORACLE_PASSES_BUT_WALKFORWARD_FAILS`：按56号建议测试事件发生当时状态能否提前判断顺势/反转。数据覆盖 `2025-06-01 00:00 UTC` 到 `2026-06-29 15:15 UTC`，`37790` 根15分钟K线，重复/断档均为 `0`。事件标签oracle能过放宽门槛：2025 `+1528.72%`、2026 `+659.73%`、最大回撤 `-38.23%`、交易 `268`、亏损月 `0`，但标签看未来不能交易。严格走步完整标签预测最好为2025 `-24.71%`、2026 `+73.97%`、最大回撤 `-41.65%`；粗动作预测最好也只有2025 `+27.13%`、2026 `+9.63%`、最大回撤 `-35.36%`。结论：尾部事件后有历史空间，但当前简单状态特征/决策树不能稳定预测未来月份动作；不要把57号升级策略，也不要继续只调树深/叶子/持仓时间。若继续，应另起58号看更贴近事件的提前信息，如 funding/premium 变化、BTC/HYPE 谁先动、事件后最初几根15分钟确认、成交量/taker 压力。
- 58号 尾部事件微观提前信息审计结论 `EVENT_MICRO_ORACLE_PASSES_BUT_WALKFORWARD_FAILS`：按57号建议加入更贴近事件本身的信息，包括 funding/premium 变化、BTC/HYPE 谁先动、事件后 `1/2/4` 根15分钟确认、成交量和 taker 压力。主审计用42号完整公共REST数据到 `2026-05`，因为49号最新2026-06 K线只有 close、缺成交量/taker。数据行数 `35168`，重复/断档均为 `0`。确认延迟oracle仍能过：最好 confirm `0`，2025 `+72596889.40%`、2026 `+2176924.83%`、最大回撤 `-31.21%`。但严格扫描 `1620` 个配置通过数为 `0`；最好严格配置为 confirm `2`、`market_only`、树深 `4`、叶子 `3`、hold `96`、2倍，2025 `+144.16%`、2026 `+151.71%`，但最大回撤 `-65.31%`；低回撤最近配置为 confirm `1`、`event_micro_plus_time`，2025 `+88.56%`、2026 `+368.95%`、最大回撤 `-47.83%`，但2025未到100%。结论：微观信息有帮助但仍不能同时满足收益和回撤；不要继续只调58号树深/叶子/持仓/杠杆/确认K。若继续，应另起59号做58号失败月份/失败事件归因，再决定是否加极简因果风控或停止尾部事件动作选择路线。
- 59号 58号失败月份和失败事件归因结论 `DRAWDOWN_SPREAD_ACROSS_MANY_EVENTS`：只复用58号结果，不新增策略规则，拆“最好收益但回撤失败”和“低回撤近似”两条代表配置。58号最好收益配置为2025 `+144.16%`、2026 `+151.71%`、最大回撤 `-65.31%`；主回撤窗口从 `2025-11-18 12:30 UTC` 到 `2026-01-29 14:00 UTC`，横跨 `39` 笔交易，其中 `20` 笔亏损，窗口内最差3笔只占窗口亏损 `39.50%`，全样本最差3笔只占总负交易亏损 `16.60%`。最差5笔全是预测动作与oracle相反，主要是 HYPE 顺势/反转判断错。低回撤近似配置最大回撤 `-47.83%`，但2025只有 `+88.56%`，不达标。结论：主回撤不是一两笔造成，简单删最差几笔或小修止损大概率救不回；若继续，应另起60号只做极简因果风控上限测试，若仍失败就停掉 BTC/HYPE 尾部事件动作选择路线。
