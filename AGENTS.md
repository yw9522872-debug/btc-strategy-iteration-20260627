# 项目工作说明

## 沟通方式

- 用户是一位 80 岁老人，尽量用中文，用最通俗的话说明。
- 除非必须确认危险操作，否则不要频繁打扰用户。
- 如需通过浏览器询问 GPT Pro，问题开头必须标明本项目路径 `C:\Users\WHR\Documents\策略迭代` 和本线程目标，避免和其他 Codex 线程混淆。
- 不混用其他线程的浏览器结论、文件路径或运行结果。

## 长期研究任务

本项目长期目标是继续研究 BTC 15m 策略。当前已经固化一个满足历史条件的研究版；后续如果继续优化，只能另起新版本，不能覆盖当前固化版。历史回测目标条件是：

- 2025 年收益率大于 100%。
- 2026 年收益率大于 100%。
- 不能使用未来函数，信号只能用当时已经收盘的数据。
- 市价开仓和平仓的总交易成本按 0.2% 计算。
- 历史回测中每个月都盈利。
- 每个月交易次数最低 10 次。

当前已固化研究版定位为 `0号策略`：`monthly_profit_lock_research_freeze_20260627`，见 `STRATEGY_0.md`、`CURRENT_STRATEGY_FREEZE.md` 和 `artifacts/strategy_freeze_monthly_profit_lock_20260627/freeze.json`。以后改参数、手续费、规则或数据时，不要覆盖这个固化版，要另起新编号、新文件夹。

0号策略是永久保留的基准策略。后续优化要复制或另开目录，例如 `artifacts/strategy_1_.../`，不能在 0号策略原目录里直接覆盖。

## GitHub 与 GPT Pro 协作

- 本项目身份：`C:\Users\WHR\Documents\策略迭代`，BTC 15m 策略迭代研究。
- GitHub 仓库：`https://github.com/yw9522872-debug/btc-strategy-iteration-20260627`。
- GitHub 仓库只用于同步本项目文件和给 GPT Pro 读取，不要混用其他项目或其他 Codex 线程的仓库。
- 给 GPT Pro 的问题开头必须同时写本地路径和 GitHub 仓库 URL。
- GPT Pro 优先阅读 `GPT_PRO_REVIEW_BRIEF.md`，再看 `AGENTS.md`、`scripts/` 和 `artifacts/*/summary.json`。
- 推送前先检查不要包含 API key、secret、token、`.env` 或实盘下单配置。

## 工作边界

- 这里只做研究、回测、检查，不下实盘订单。
- 不读取、不打印、不保存 API key、secret、token 或 `.env`。
- 不启动实盘 supervisor。
- 不改真实仓位。

## 验证要求

- 任何候选策略都要输出年度、月度、交易次数和手续费口径。
- 发现结果太好时，优先检查是否有未来函数或标签泄漏。
- “每个月都盈利”只能表示历史回测月份盈利，不能承诺未来真实市场一定盈利。

## 本次开发需要记住

- 当前固化版历史上满足任务条件，但参数是事后从历史里挑出来的，过拟合风险高；不能把它说成稳健实盘策略。
- 已做过 2024 选参、2025/2026 验证：这个更严格检查没有达到每年 100%，说明过拟合风险是真问题。
- 当前固化版执行逻辑没有发现明显未来函数：信号只用已收盘K线，下一根K线才吃收益；月度锁利只用当月已经发生的收益和交易次数。
- 手续费口径固定为开平合计 0.2%，代码里是单边 `0.001`。
- 2026 主结果用本地数据到 `2026-06-19 23:45 UTC`；另有 Binance 补测到 `2026-06-26 18:30 UTC`，补测后月内已锁利空仓。
- 胜率默认口径是“持仓中的 15分钟K线正收益占比”，不是严格的一笔一笔完整交易胜率。
- 14号发现旧 `event_entry_fullscan` 对齐 Binance USD-M futures 公共K线，不是 spot；用 2020-2026 更严格滚动审计后，`ret_state 64/100` 家族判定为 `STOP_FAMILY`，不要继续手工堆补丁。
- 15号已确认统一 futures K线底座可用：`2020-01-01 00:00 UTC` 到 `2026-05-31 23:45 UTC`，`224928` 根15分钟K线，重复/断档/OHLC异常/缺失月份均为 `0`。后续新策略族优先基于这个底座。
- 16号在15号底座上扫了简单新策略族（均线/Donchian/RSI/布林带/ATR突破），严格逐月选择没有硬通过，144个静态候选事后硬通过数量也是 `0`。不要把16号升级候选；下一步更适合做上限测试。
- 17号对16号候选做了“看答案”的月度上限测试，仍然失败：最宽松上限还有 `6` 个不盈利月份，要求每月交易不少于 `10` 后有 `10` 个不盈利月份。结论 `SIMPLE_FAMILY_UPPER_BOUND_FAILS`，不要继续扩这批简单规则。
- 18号复盘17号失败月份：`6` 个月是144个候选没有任何正收益，`4` 个月是有正收益但交易次数不足10。结论 `FAILURE_MONTHS_EXPLAIN_SIMPLE_FAMILY_STOP`，说明这批简单规则不值得继续微调。
- 19号测试日历/星期/小时季节性，严格逐月选择也失败：最好 `all_calendar` 为 2023 `-1.32%`、2024 `+71.64%`、2025 `-26.91%`、2026 YTD `+43.60%`，亏损月 `22`。结论 `CALENDAR_SEASONALITY_FAILS`，不要升级候选。
- 20号测试 OHLC K线实体/影线/振幅/波动结构的“看答案”上限，仍然失败：189个候选静态硬通过为 `0`；最宽松月度上限 2023 `+51.61%`、2024 `+18.35%`、2025 `+12.99%`、2026 YTD `+0.18%`，仍有 `30` 个不盈利月份；要求每月10单后 2024/2025/2026 都亏。结论 `OHLC_STRUCTURE_UPPER_BOUND_FAILS`，不要升级这批 OHLC 结构规则。
- 21号补 Binance USD-M futures 15m 成交量/taker 字段做“看答案”上限，仍然失败：378个候选静态硬通过为 `0`；最宽松月度上限 2023 `+45.98%`、2024 `+54.74%`、2025 `-12.01%`、2026 YTD `-9.17%`，仍有 `25` 个不盈利月份；要求每月10单后有 `29` 个不盈利月份。结论 `VOLUME_UPPER_BOUND_FAILS`，不要升级这批成交量小规则。
- 22号把 16/19/20/21 的 `927` 个候选放入同一张硬目标压力表：严格逐月选择器 `0/120` 通过；原始硬目标下，月度oracle也没过，差 `2023-07` 和 `2024-06` 两个月。结论 `ORIGINAL_TARGET_AND_SELECTION_BOTTLENECK`。不要继续扩 16/19/20/21 小规则；若继续研究，只能换真正不同的数据源，或先把目标改得更现实。
- 23号测试 Binance USD-M futures 资金费率这个新数据源的“看答案”上限：246个资金费率候选静态硬通过为 `0`，但月度oracle带每月10单门槛能过，2023 `+17263.32%`、2024 `+45467.21%`、2025 `+6801.02%`、2026 YTD `+648.74%`，亏损月 `0`。结论 `FUNDING_RATE_UPPER_BOUND_HAS_MONTHLY_PIECES`，但不能交易。
- 24号复用23号资金费率候选做严格逐月选择，失败：最好 `funding_mean_only` 为 2023 `-22.82%`、2024 `-42.05%`、2025 `+9.70%`、2026 YTD `+10.95%`，亏损月 `20`。结论 `FUNDING_RATE_STRICT_SELECTOR_FAILS`。不要升级资金费率候选；若继续，应换持仓量等新数据源先做上限测试，或调整硬目标。
- 25号检查 Binance 公开持仓量数据能否做 2020-2026 上限测试，结论 `OPEN_INTEREST_HISTORY_NOT_AVAILABLE_FOR_2020_2026`：openInterestHist 最近数据可取，但 2020-01、2023-01 历史请求返回 `startTime invalid`，公开接口只够最近1个月观察，不够做 2023-2026 硬目标上限。不要用不完整持仓量硬做历史上限。
- `DATA_SOURCE_OPEN_INTEREST_LONG_SHORT_REVIEW_20260627.md` 已审查持仓量/多空比历史数据源：Binance 官方持仓量只保留最近1个月，多空比只保留最近30天；首选 Tardis.dev，其次 CoinGlass/Amberdata。拿到完整 CSV 前，不要硬做持仓量/多空比多年回测；拿到后先做数据质量审计，再做上限测试。
- 26号改用 Binance 免费公开 1分钟 USD-M futures K线测试15分钟内部结构上限，结论 `INTRABAR_1M_UPPER_BOUND_FAILS`：2020-01到2026-05 的1分钟月包完整，缺失/重复/断档均为 `0`；有 `8` 根15分钟K线和15号底座OHLC不一致，已禁用这些K线信号。186个候选静态硬通过为 `0`；最好看答案上限 2023 `+47.27%`、2024 `+16.29%`、2025 `-3.27%`、2026 YTD `-7.68%`，亏损月 `28`；要求每月10单后 2025/2026 仍亏。不要继续扩这批1分钟内部结构小规则。
- 27号目标可行性审计结论 `TARGET_RELAXATION_HELPS_ORACLE_NOT_SELECTOR`：复用22号927个候选，测试年收益30/50/80/100、月度不限/不低于-2%/不低于-1%/>0、月交易0/5/10。严格逐月选择器 `0/49` 通过；看答案 oracle `37/49` 通过。原手续费0.2%下，看答案最接近原目标的通过口径是年100%、每月10单、月亏不低于-1%；但严格选择器仍失败。不要继续加免费K线小规则；若不拿真正不同的新数据，应把目标降到影子跟踪/低年化验证。
- 28号不要求月月盈利审计结论 `NO_STRICT_RELAXED_UPGRADE`：复用14号 `ret_state 64/100` 家族和统一期货K线，不新增规则、不新增数据。去掉每月必须盈利后，最好的严格不看未来选择器仍只有 2023 `-21.96%`、2024 `+140.23%`、2025 `-5.28%`、2026 YTD `-0.35%`，亏损月份 `6`，最大回撤 `-58.39%`。看答案月度 oracle 可以做到 2025 `+1692.64%`、2026 YTD `+171.90%`，但这是未来函数，不能交易。结论：问题不是月月盈利条件单独太严，而是当月之前选不出正确候选。
- 29号按 GPT Pro 建议检查免费 raw trade 数据覆盖，结论 `FREE_SPOT_PERP_RAW_TRADE_DATA_AVAILABLE`：Binance 免费公开 futures/spot `aggTrades`、`trades`、1m K线、fundingRate、markPriceKlines、indexPriceKlines、premiumIndexKlines 从 `2020-01` 到 `2026-05` 均为 `77/77` 个月可用；bookTicker 月包探针 2020-01 返回 `404`，不要做秒级盘口主策略。下一步若继续，另起30号，只做 spot-perp `aggTrades` 成交流错位上限测试，funding/mark/index/premium 只做过滤器。
